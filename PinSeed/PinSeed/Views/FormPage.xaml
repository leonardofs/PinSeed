<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PinSeed.Views.FormPage">

    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness" iOS="0,0,0,0" Android="0,0,0,0" />
    </ContentPage.Padding>

    <ContentPage.Content>

        <StackLayout
            Spacing="0"
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand">

            <!--<Image 
                    AbsoluteLayout.LayoutBounds="0.5, 0.5, 0.36, 0.7"
                    AbsoluteLayout.LayoutFlags="All"
                    BackgroundColor="Transparent" 
                    HeightRequest="36"
                    Source="{Binding Imagem}"/>-->
            <Button Text="IAI MEN" HorizontalOptions="Center" VerticalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>