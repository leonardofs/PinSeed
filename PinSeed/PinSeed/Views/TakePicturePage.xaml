<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PinSeed.Views.TakePicturePage"
             Title="Take Picture">
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness" iOS="0,0,0,0" Android="0,0,0,0" />
    </ContentPage.Padding>
    
    <StackLayout>
        
        <StackLayout Orientation="Horizontal"  VerticalOptions="End">

            <ActivityIndicator x:Name="actInd"
                             IsRunning="{Binding IsLoading}"
                             IsVisible="{Binding IsLoading}" Color="Red"/>


            <Button x:Name="takePhoto" Text="Tirar foto" IsEnabled="{Binding EnableButtons}" Command="{Binding TakePictureCommand }" BackgroundColor="Pink"  BorderColor="Transparent"/>
            <Button x:Name="pickPhoto" Text="selecionar foto" IsEnabled="{Binding EnableButtons}" Command= "{Binding SelectPictureCommand}" BackgroundColor="Pink"  BorderColor="Transparent"/>
        </StackLayout>
    </StackLayout>
</ContentPage>